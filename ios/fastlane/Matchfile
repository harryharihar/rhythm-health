git_url(ENV['MATCH_GIT_URL'])
storage_mode('git')

# app_identifier and type are passed explicitly per match() call in the
# Fastfile since Dev and Prod use different bundle ids and profile types
# (development vs appstore).
